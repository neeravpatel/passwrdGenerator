# passwrdGenerator

## Creates good strong password

A strong password has:
- at least 15 characters
- uppercase letters
- lowercase letters
- numbers
- symbols, such as ` ! ” ? $ ? % ^ & * ( ) _ – + = { [ } ] : ; @ ‘ ~ # | < , > . ? /

